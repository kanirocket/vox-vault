# Backend target for the dev server's /api proxy (defaults to :4031).
# The Google client id is NOT set here — the frontend fetches it at runtime
# from the backend (/api/auth/config). Configure GOOGLE_CLIENT_ID on the
# backend instead (root .env / docker compose environment).
VITE_API_TARGET=http://localhost:4031
